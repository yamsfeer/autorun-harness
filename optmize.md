-[] 评估任务完成情况时, 总是报警告" ⚠️  未找到评估报告，生成默认报告"
-[] 优化: planner 和 generator 执行过程中的输出,总是包含一个 emoji 图标, 删除它或选择更好看的方式
-[] init 和 run 连续执行, 可以添加命令,或直接 autorun-harness init && autorun-harness run
-[] max-iteration 默认是 10 个 task, 可以默认全部执行
-[] 似乎当达到限额之后,没有自动切换模型,从日志来看,一般都是直接"开始实现","开始评估",没有其他输出,我怀疑是没有任何实现就交给evaluator,然后直接就通过了.
